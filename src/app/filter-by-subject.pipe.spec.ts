import { FilterBySubjectPipe } from './filter-by-subject.pipe';

describe('FilterBySubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
